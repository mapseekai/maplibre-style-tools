# Standalone MapLibre Style Tools Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Produce a history-preserving, standalone `maplibre-style-tools` Git project at `/Users/zhang/code/maplibre-style-tools` that builds declarations and JavaScript, passes its existing tests, and packs from `dist` without modifying `ai-style-editor`.

**Architecture:** Keep the extracted `src/` tree and public API byte-for-byte unchanged. Add a minimal single-package pnpm/TypeScript/ESLint toolchain around it, compile ESM with `NodeNext` into `dist`, compile existing `node:test` tests separately into `.tmp/test-dist`, then move the verified repository from its writable staging directory to the requested destination.

**Tech Stack:** TypeScript 5.9.3, Node.js ESM and `node:test`, pnpm 10.10.0, ESLint 9 flat config, AI SDK 6, Zod 4, MapLibre GL JS 5.

## Global Constraints

- Final repository path: `/Users/zhang/code/maplibre-style-tools`.
- Final project and npm package name: `maplibre-style-tools`.
- Preserve subtree-split Git history and use `main` as the branch.
- Preserve every file under `src/` without implementation or API changes.
- Keep `/Users/zhang/code/ai-style-editor` unchanged, including its original workspace package and consumer.
- Do not publish, add a remote, configure CI, or add a license.
- Package consumers load `dist/index.js` and `dist/index.d.ts`, never raw TypeScript.
- Retain `maplibre-gl` as a peer dependency and add it as a development dependency.

---

### Task 1: Add a Standalone Build, Test, Lint, and Package Contract

**Files:**
- Modify: `package.json`
- Create: `tsconfig.json`
- Create: `tsconfig.build.json`
- Create: `tsconfig.test.json`
- Create: `eslint.config.js`
- Create: `.gitignore`
- Create: `README.md`
- Create: `pnpm-lock.yaml` (generated by pnpm)

**Interfaces:**
- Consumes: the unchanged `src/index.ts` ESM root entry and its existing `.js` relative specifiers.
- Produces: `dist/index.js`, `dist/index.d.ts`, package root export `.`, and scripts `lint`, `typecheck`, `build`, `test`, and `prepack`.

- [ ] **Step 1: Record the expected pre-configuration failure**

Run:

```bash
pnpm run build
```

Expected: failure stating that the `build` script is missing. This proves the extracted snapshot is not yet independently buildable.

- [ ] **Step 2: Replace the workspace-oriented manifest**

Replace `package.json` with:

```json
{
  "name": "maplibre-style-tools",
  "version": "0.0.0",
  "description": "AI SDK tools for inspecting and editing MapLibre styles.",
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "prebuild": "tsc -b tsconfig.build.json --clean",
    "build": "tsc -b tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "tsc -p tsconfig.test.json",
    "posttest": "node --test .tmp/test-dist/engine/style-context.test.js .tmp/test-dist/engine/style-operations.test.js",
    "prepack": "pnpm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@10.10.0",
  "peerDependencies": {
    "maplibre-gl": "^5.21.1"
  },
  "dependencies": {
    "@maplibre/maplibre-gl-style-spec": "^24.7.0",
    "ai": "^6.0.141",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@types/node": "^24.12.0",
    "eslint": "^9.39.4",
    "maplibre-gl": "^5.21.1",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.57.0"
  }
}
```

- [ ] **Step 3: Add strict TypeScript configurations**

Create `tsconfig.json`:

```json
{
  "compilerOptions": {
    "target": "ES2023",
    "lib": ["ES2023", "DOM"],
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "types": ["node"],
    "strict": true,
    "skipLibCheck": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noUncheckedSideEffectImports": true,
    "noEmit": true
  },
  "include": ["src/**/*.ts"],
  "exclude": ["dist", ".tmp", "node_modules"]
}
```

Create `tsconfig.build.json`:

```json
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "composite": true,
    "rootDir": "./src",
    "outDir": "./dist",
    "tsBuildInfoFile": "./.tmp/tsconfig.build.tsbuildinfo",
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  },
  "include": ["src/**/*.ts"],
  "exclude": ["src/**/*.test.ts", "dist", ".tmp", "node_modules"]
}
```

Create `tsconfig.test.json`:

```json
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "rootDir": "./src",
    "outDir": "./.tmp/test-dist",
    "declaration": false,
    "declarationMap": false,
    "sourceMap": true
  },
  "include": ["src/**/*.ts"],
  "exclude": ["dist", ".tmp", "node_modules"]
}
```

- [ ] **Step 4: Add library-only linting and generated-file ignores**

Create `eslint.config.js`:

```javascript
import js from '@eslint/js';
import tseslint from 'typescript-eslint';
import { defineConfig, globalIgnores } from 'eslint/config';

export default defineConfig([
  globalIgnores(['dist', '.tmp', 'node_modules']),
  {
    files: ['**/*.ts'],
    extends: [js.configs.recommended, tseslint.configs.recommended],
    languageOptions: {
      ecmaVersion: 2023,
    },
  },
]);
```

Create `.gitignore`:

```gitignore
node_modules/
dist/
.tmp/
*.tsbuildinfo
*.tgz
```

- [ ] **Step 5: Document both public factories and local development**

Create `README.md`:

````markdown
# maplibre-style-tools

AI SDK tools for inspecting and editing MapLibre GL styles.

The package exposes a compact tool set for token-efficient workflows and a full tool set for broad style editing. It is currently maintained as a standalone local project and has not been published to npm.

## Requirements

- Node.js 18 or newer
- `maplibre-gl` 5.21 or a compatible release
- An AI SDK 6 tool consumer

## Local installation

Build the package once, then add it from a sibling project:

```bash
cd ../maplibre-style-tools
pnpm install
pnpm run build
cd ../your-project
pnpm add ../maplibre-style-tools
pnpm add maplibre-gl
```

## Compact tools

```ts
import { createCompactMapLibreStyleTools } from 'maplibre-style-tools';

const tools = createCompactMapLibreStyleTools({
  getMap: () => map,
  getContext: () => ({
    activeSourceId: 'basemap',
    selectedLayerId: 'road-primary',
  }),
});
```

The compact factory provides style context, layer search and inspection, validated batch operations, and patch JSON validation.

## Full tools

```ts
import { createMapLibreStyleTools } from 'maplibre-style-tools';

const tools = createMapLibreStyleTools({
  getMap: () => map,
});
```

The full factory exposes detailed tools for layers, sources, paint and layout properties, filters, zoom ranges, metadata, camera, terrain, sprites, glyphs, images, and other MapLibre style features.

## Development

```bash
pnpm install
pnpm run lint
pnpm run typecheck
pnpm run build
pnpm test
npm pack --dry-run
```

Build output is written to `dist/`. Tests use Node's built-in test runner and compile into `.tmp/test-dist/`.
````

- [ ] **Step 6: Install dependencies and generate the standalone lockfile**

Run:

```bash
pnpm install
```

Expected: `pnpm-lock.yaml` is created with a single root importer, dependencies install successfully, and no workspace link to `ai-style-editor` appears.

Then run:

```bash
pnpm install --frozen-lockfile
```

Expected: success without changing `pnpm-lock.yaml`.

- [ ] **Step 7: Verify static checks, compilation, and existing behavior**

Run each command separately:

```bash
pnpm run lint
pnpm run typecheck
pnpm run build
pnpm test
```

Expected: all commands exit successfully; `dist/index.js` and `dist/index.d.ts` exist; Node reports four passing unit tests and zero failures.

- [ ] **Step 8: Verify the generated public entry point and package contents**

Run:

```bash
node --input-type=module --eval "const m = await import('./dist/index.js'); if (typeof m.createMapLibreStyleTools !== 'function' || typeof m.createCompactMapLibreStyleTools !== 'function') process.exit(1)"
```

Expected: exit code 0 with no output.

Run:

```bash
npm pack --dry-run --json
```

Expected: success; the JSON file list contains `dist/index.js`, `dist/index.d.ts`, their maps and compiled internal modules, plus `README.md` and `package.json`; it does not publish `src/`.

- [ ] **Step 9: Review and commit the standalone project configuration**

Run:

```bash
git diff --check
git status --short
```

Expected: no whitespace errors; only the intended configuration, documentation, and lockfile files are changed or untracked.

Commit:

```bash
git add .gitignore README.md eslint.config.js package.json pnpm-lock.yaml tsconfig.json tsconfig.build.json tsconfig.test.json
git commit -m "build: make package independently buildable"
```

### Task 2: Prove Source Preservation and Place the Repository

**Files:**
- Verify unchanged: `src/**`
- Move repository: `/private/tmp/maplibre-style-tools-staging.GJZr1M/project` → `/Users/zhang/code/maplibre-style-tools`

**Interfaces:**
- Consumes: the verified standalone project and source package at `/Users/zhang/code/ai-style-editor/packages/maplibre-style-tools`.
- Produces: the final standalone Git repository at the requested absolute path, with no remote and a clean `main` branch.

- [ ] **Step 1: Prove the implementation source is byte-for-byte unchanged**

Run:

```bash
diff -ru /Users/zhang/code/ai-style-editor/packages/maplibre-style-tools/src ./src
```

Expected: exit code 0 and no output.

- [ ] **Step 2: Verify extracted history and remote isolation**

Run:

```bash
git log --oneline --decorate -5
git remote -v
```

Expected: the two original package commits are followed by the design, plan, and standalone build commits; `git remote -v` prints nothing.

- [ ] **Step 3: Confirm the destination is still absent**

Run:

```bash
test ! -e /Users/zhang/code/maplibre-style-tools
```

Expected: exit code 0. Stop rather than overwrite if the path exists.

- [ ] **Step 4: Move the verified repository to its final location**

Run with the required filesystem approval:

```bash
mv /private/tmp/maplibre-style-tools-staging.GJZr1M/project /Users/zhang/code/maplibre-style-tools
```

Expected: the repository now exists at `/Users/zhang/code/maplibre-style-tools`; the source monorepo remains in place.

- [ ] **Step 5: Verify final repository and source repository state**

From `/Users/zhang/code/maplibre-style-tools`, run:

```bash
git status --short --branch
git log --oneline --decorate -5
git remote -v
```

Expected: clean `main` branch, expected history, and no remote.

From `/Users/zhang/code/ai-style-editor`, run:

```bash
git diff --exit-code -- packages/maplibre-style-tools package.json pnpm-workspace.yaml pnpm-lock.yaml tsconfig.test.json src/ChatInterface.tsx
```

Expected: exit code 0, proving no tracked source-workspace files were changed.
